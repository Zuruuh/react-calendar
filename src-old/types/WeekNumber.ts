// TODO: Make this type take a generic and be type safe depending on overlay mode
export type WeekNumber = [number] | [number, number];
export type WeekNumbers = Array<[number]> | Array<[number, number]>;
